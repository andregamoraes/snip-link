# Copy this to your frontend .env.local file
# Base URL for the backend API used by the frontend
# Only needed if you are running the project without Docker Compose
NEXT_PUBLIC_API_URL=http://localhost:4000
